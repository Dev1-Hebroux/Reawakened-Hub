# Fly.io Environment Variables Template
# Run: fly secrets set KEY=value

# === REQUIRED ===

# Database connection (Supabase PostgreSQL)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Session secret for cookie signing (generate with: openssl rand -hex 32)
SESSION_SECRET=your-32-character-secret-here

# === OPTIONAL - AI Features ===

# OpenAI API key for AI coach and text-to-speech
OPENAI_API_KEY=sk-your-openai-api-key

# === OPTIONAL - Email ===

# Resend API key for transactional emails
RESEND_API_KEY=re_your_resend_api_key

# Prayer team notification email
PRAYER_TEAM_EMAIL=prayer@yourdomain.com

# === OPTIONAL - Push Notifications ===

# VAPID keys for web push notifications (generate at https://vapidkeys.com/)
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_SUBJECT=mailto:hello@yourdomain.com

# === OPTIONAL - Admin Access ===

# Comma-separated list of super admin emails
SUPER_ADMIN_EMAILS=admin@yourdomain.com

# === OPTIONAL - File Storage ===

# Google Cloud Storage configuration (if migrating from Replit Object Storage)
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_BUCKET=your-bucket-name
GOOGLE_CLOUD_KEYFILE=path/to/keyfile.json

# === CLIENT-SIDE VARIABLES ===
# These need to be set at build time in Dockerfile or fly.toml

# VITE_API_URL - Only set if API is on different domain (leave empty for same-origin)
